declare module "webaudiofont" {
    // The npm package is plain JS with no bundled types.
    // WebAudioFontPlayer is a constructor function; treat it as `any`.
    const WebAudioFontPlayer: any;
    export default WebAudioFontPlayer;
}
